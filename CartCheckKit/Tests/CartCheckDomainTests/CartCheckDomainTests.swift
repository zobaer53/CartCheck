import Testing
@testable import CartCheckDomain

@Suite("CartCheckDomain scaffold")
struct CartCheckDomainScaffoldTests {
    @Test("package builds and links")
    func packageLinks() {
        #expect(true)
    }
}
